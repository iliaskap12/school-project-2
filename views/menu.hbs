<nav class="categories" aria-label="secondary-menu">
    <h2 class="title">Κατηγορίες:</h2>
    <ul class="category-list">
        {{#each categories}}
            <li class="category" id="{{this.id}}">{{this.title}}</li>
        {{/each}}
    </ul>
</nav>