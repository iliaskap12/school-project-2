{
  "name": "school-project-2",
  "version": "0.0.0",
  "main": "app.js",
  "scripts": {
    "lint": "prettier-standard '**/*.{js, css, html, json, hbs}' --lint",
    "format": "prettier-standard '**/*.{js, css, html, json, hbs}' --format",
    "start": "npm run lint && npm run format && nodemon app.js"
  },
  "dependencies": {
    "express": "~4.16.1",
    "mongodb": "^4.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prettier-standard": "^13.0.6"
  }
}
